---
---

<div class="container">
  <div class="empty">
    <p class="empty__eyebrow">Portfolio</p>
    <h2 class="empty__title">No projects yet.</h2>
    <p class="empty__text">
      Add your first costume design project in the Studio, then refresh this page to see it appear here.
    </p>
    <a
      class="empty__cta"
      href="http://localhost:3333/"
      target="_blank"
      rel="noopener noreferrer"
    >
      Open Sanity Studio
    </a>
  </div>
</div>

<style>
  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: var(--space-6) var(--space-3);
  }

  .empty {
    max-width: 520px;
    text-align: center;
    padding: var(--space-6) var(--space-4);
    border-radius: 28px;
    background: rgba(255, 255, 255, 0.78);
    box-shadow: 0 24px 60px rgba(18, 12, 8, 0.14);
    border: 1px solid rgba(255, 255, 255, 0.6);
  }

  .empty__eyebrow {
    font-family: var(--font-family-mono);
    text-transform: uppercase;
    letter-spacing: 0.24em;
    font-size: 12px;
    color: var(--accent-700);
    margin: 0 0 var(--space-3);
  }

  .empty__title {
    font-family: var(--font-family-display);
    font-size: clamp(32px, 4vw, 44px);
    margin: 0 0 var(--space-2);
  }

  .empty__text {
    font-family: var(--font-family-serif);
    font-size: 17px;
    line-height: 1.7;
    color: var(--gray-700);
    margin: 0 0 var(--space-4);
  }

  .empty__cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 12px 22px;
    border-radius: 999px;
    text-decoration: none;
    font-family: var(--font-family-mono);
    font-size: 12px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    background: var(--accent-700);
    color: white;
    box-shadow: 0 12px 24px rgba(139, 67, 38, 0.35);
  }
</style>
